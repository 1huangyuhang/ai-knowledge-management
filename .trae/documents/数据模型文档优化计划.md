## 数据模型文档优化计划

### 1. 修复核心问题

#### 1.1 添加Suggestion表定义
- 在数据模型定义文档中添加Suggestion表的完整定义
- 包含id、modelId、type、content、priority、status等字段
- 定义适当的外键约束和索引

#### 1.2 完善ThoughtFragment实体的外键约束
- 明确ThoughtFragment.inputId字段与FileInput、SpeechInput等表的关联关系
- 考虑使用多态关联或单独的关联表

#### 1.3 添加文本输入实体
- 添加TextInput实体，用于存储用户直接输入的文本信息
- 定义与AITask和ThoughtFragment的关联关系

#### 1.4 添加Suggestion与CognitiveInsight的关联
- 在Suggestion表中添加relatedInsightIds字段
- 定义适当的索引和约束

#### 1.5 添加建议反馈机制
- 添加SuggestionFeedback实体，用于存储用户对建议的反馈
- 包含suggestionId、rating、feedback、action等字段

### 2. 优化文档完整性

#### 2.1 更新文档更新记录
- 将数据模型定义文档的更新日期改为当前日期
- 记录所有修改内容

#### 2.2 完善业务规则
- 为Suggestion实体添加业务规则
- 确保所有实体都有明确的业务规则定义

#### 2.3 优化实体关系图
- 更新实体关系图，包含Suggestion和TextInput实体
- 明确所有实体之间的关系

### 3. 确保一致性

#### 3.1 与领域层设计保持一致
- 确保数据模型与领域层设计文档中的实体定义一致
- 统一字段名称、类型和约束

#### 3.2 与API规范保持一致
- 确保数据模型支持API规范中定义的所有功能
- 包含API使用示例中提到的所有实体

### 4. 提高可扩展性

#### 4.1 考虑未来扩展需求
- 设计灵活的数据模型，支持未来功能扩展
- 避免过度设计，保持模型简洁

#### 4.2 添加适当的索引
- 为频繁查询的字段添加索引
- 优化查询性能

### 5. 完善文档结构

#### 5.1 添加实体描述
- 为每个实体添加详细的描述
- 说明实体的用途和业务逻辑

#### 5.2 完善字段说明
- 为每个字段添加详细的说明
- 说明字段的用途、约束和默认值

#### 5.3 优化文档格式
- 确保文档格式一致
- 使用清晰的标题和结构

通过以上优化，数据模型定义文档将更加完整、一致和准确，能够更好地支持后续的开发工作，使AI生成代码更加容易，且符合规范。